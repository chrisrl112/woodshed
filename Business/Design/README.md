# Design

UX/visual direction and design decisions and rationale, at the level above individual build specs in `docs/`.

Use this for design language, aesthetic direction, and "why we designed it this way" — not pixel-level specs tied to a specific build (those stay in `docs/`).
